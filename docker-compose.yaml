version: "3"
services:
  nginx:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 80:80
    # volumes:
    #   - ./nginx.conf:/etc/nginx/nginx.conf
    # 如果有其他需要的自定义配置文件，请根据实际情况进行添加
    # - ./custom.conf:/etc/nginx/conf.d/custom.conf
